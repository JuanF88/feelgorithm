{
  "name": "feelgorithm",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Juego web MIL — UNESCO Youth Hackathon 2026",
  "scripts": {
    "dev": "npx --yes browser-sync start --server . --port 4321 --files \"index.html, src/**/*.js, assets/**/*\" --no-notify --no-open",
    "start": "npx --yes serve -l 4321 ."
  }
}
